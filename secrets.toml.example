# Copy this block into Streamlit Community Cloud → your app → Settings → Secrets.
# Do not commit real values. Rename is only for local reference; Cloud expects TOML pasted in the UI.

MONGO_CONNECTION_STRING = "mongodb+srv://USER:PASSWORD@cluster.example.mongodb.net/?appName=Cluster0"
GROQ_API_KEY = "gsk_..."
